{
    "name": "ubiquity-worktime-monitor",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "dev": "npx dotenv-cli -e .env -- concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
        "dev:frontend": "npm run dev --prefix frontend",
        "install": "npm install --prefix frontend && npm install --prefix backend",
        "build": "node scripts/build.js",
        "start": "npm run start --prefix backend",
        "dev:backend": "npx dotenv-cli -e .env -- npm run dev --prefix backend",
        "prisma:generate": "npx dotenv-cli -e .env -- npm run prisma:generate --prefix backend",
        "prisma:migrate:dev": "npx dotenv-cli -e .env -- npm run prisma:migrate:dev --prefix backend",
        "prisma:migrate:deploy": "npx dotenv-cli -e .env -- npm run prisma:migrate:deploy --prefix backend"
    },
    "author": "Leczok12",
    "license": "ISC",
    "description": "",
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
